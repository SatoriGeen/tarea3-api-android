# API REST con Retrofit

## Descripción
la tarea presnetada demuestra el consumo de una API RESTful local (dockerizada) utilizando la libreria **Retrofit**

## Ejercicio 1: conexión y verificación de la API
Se configuro el cliente HTTP y los permisos de red (`usesCleartextTraffic`) para realizar una petición `GET` al endpoint raíz (`/`). 

### Evidencia ejercicio 1:
